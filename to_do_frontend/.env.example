# Frontend environment configuration for Simple To‑Do (React)
# Copy this file to ".env" (development) or ".env.production" (production) and adjust values.
# Note: Create React App only exposes variables prefixed with REACT_APP_ to the browser.

# Preferred base URL for all API requests made by the frontend.
# Example for local backend dev server:
REACT_APP_API_BASE=http://localhost:4000

# Alternate backend URL (legacy/backup). If REACT_APP_API_BASE is set, it takes precedence.
REACT_APP_BACKEND_URL=http://localhost:4000

# The public URL where this frontend is hosted (useful for CORS, redirects, links).
REACT_APP_FRONTEND_URL=http://localhost:3000

# Optional: WebSocket base URL if your backend exposes WS endpoints.
# Not currently used in this app, provided for future expansion.
REACT_APP_WS_URL=ws://localhost:4000

# Node environment indicator used by the frontend for behavior toggles/logging.
# Common values: development | production | test
REACT_APP_NODE_ENV=development

# Frontend log verbosity level (app-level). Suggested values: debug | info | warn | error
REACT_APP_LOG_LEVEL=info

# Optional CRA-related flags (provided for completeness; not all are used)
# Disable Next.js telemetry style flag (harmless in CRA; documented for consistency)
REACT_APP_NEXT_TELEMETRY_DISABLED=1
# Enable or disable source maps in production builds
REACT_APP_ENABLE_SOURCE_MAPS=true
# Port used by the frontend dev server
REACT_APP_PORT=3000
# Trust proxy setting for reverse-proxy scenarios (string flag)
REACT_APP_TRUST_PROXY=false
# Healthcheck path for uptime monitors (frontend route)
REACT_APP_HEALTHCHECK_PATH=/health
# Comma-separated feature flags toggles (e.g., new-ui,experiment-x)
REACT_APP_FEATURE_FLAGS=
# Enable experiments (boolean-like)
REACT_APP_EXPERIMENTS_ENABLED=false

# Precedence notes for backend URL resolution in the frontend:
# 1) REACT_APP_API_BASE (preferred)
# 2) REACT_APP_BACKEND_URL
# 3) Fallback to same-origin "" (useful when a dev proxy is configured)
